# RK3506G2 openvela 移植 — 演示视频脚本

**时长**: 4 分 30 秒
**格式**: 屏幕录制 + 串口终端 + 实物镜头

---

## 开场（0:00 - 0:20）

### 画面

开发板实物特写，LED 亮起，串口终端显示 NSH 提示符。

### 旁白

> 各位Openvela的老师，评委们好，我是 MingchengNetworkStudio 的队长夏嘉铭。我的队伍编号是309，我是新硬件移植的赛道，我的项目是 RK3506G2 芯片的 openvela 的移植。
>
> RK3506G2 是瑞芯微的低功耗 IoT 芯片，采用 3 个 Cortex-A7 加 1 个 Cortex-M0 异构架构，配备 128MB DDR3 内存。
>
> 我的移植工作包含 HD-RK3506G-EVM的板级适配和RK3506G2的Chip适配，除屏幕等没有条件测试的情况外，均进行了测试

---

## 第一部分：系统启动（0:20 - 0:50）

### 操作命令

```bash
# 串口终端已连接，开发板已上电
nsh> uname -a
nsh> free


nsh> ps
```

### 旁白

> 首先看系统启动。串口终端以 115200 波特率连接，开发板上电后自动进入 NSH 命令行。
>
> `uname -a` 显示系统信息，确认是 NuttX 内核。
>
> `free` 显示内存使用情况，128MB DDR3 可用约 27MB。
>
> `ps` 显示运行中的进程，包括 NSH 主进程、NTP 守护进程、网络配置守护进程等。

---


## 第二部分：网络功能（0:50 - 1:40）

### 操作命令

```bash
nsh> ifconfig
nsh> ping 192.168.10.1
nsh> ping -c 3 stdl.b4qaq.cn
nsh> date
nsh> curl --max-time 20 -o /tmp/info.json https://stdl.b4qaq.cn/fwtb/info.json; echo DONE=$?
nsh> cat /tmp/info.json
```

### 旁白

> 接下来展示网络功能。`ifconfig` 显示 eth0 已通过 DHCP 获取 IP 地址。启动阶段交换机端口的 STP 会丢弃前十几秒的 BOOTP 广播，我们把 DHCP 重试次数加到 10 次覆盖这个窗口，所以冷启动也能稳定拿到地址。
>
> `ping 192.168.10.1` 测试局域网连通性，延迟约 2 毫秒。
>
> `ping stdl.b4qaq.cn` 测试 DNS 解析和外网连通性。
>
> `date` 显示系统时间已通过 NTP 自动同步——板子没有电池 RTC，启动时时钟停在 1970，我们在 rcS 里启动了指数退避的 NTP 守护进程，网络一通过就同步。
>
> HTTPS 的 curl 是这一层最硬的骨头，我们逐一补齐了四个根因：时间不对导致证书"来自未来"、没有 nameserver 导致 DNS 失败、系统缺 CA 根证书包、`/dev/urandom` 缺失导致 TLS 熵源初始化失败。现在 curl 可以正常完成 TLS 握手并拿到 JSON，命令里的 `--max-time` 是演示录制时的保险。

---

## 第三部分：USB Host 功能（1:40 - 2:20）

### 操作命令

```bash
# 插入 U 盘
nsh> ls /dev/sda
nsh> mount -t vfat /dev/sda /mnt/usb
nsh> ls /mnt/usb
nsh> cat /mnt/usb/test.txt
nsh> umount /mnt/usb
```

### 旁白

> 接下来展示 USB Host 功能。插入 U 盘后，系统自动枚举出 /dev/sda 块设备。
>
> `mount -t vfat` 挂载 U 盘的 FAT 文件系统。
>
> `ls` 和 `cat` 读取 U 盘内容。
>
> 这里解决了 USB bulk 传输卡死的问题——DWC2 RX FIFO 默认 128 words 太小，提到 SDK 值 512/256/224 后正常工作。

---

## 第四部分：rpmsg 多核通信（2:20 - 3:00）

> ⚠️ **录制前提**：以板上 `rpmsgtest` 实际 PASS 的固件版本为准（该功能在 v8h 版本曾完整验收通过，后期重构为 U-Boot 加载流程后仍在联调；未 PASS 前本段不录）。

### 操作命令

```bash
# 上电时 U-Boot 阶段串口先打印:
# Handle standalone: 'mcu' at 0xfff84000 ... OK
nsh> rpmsgtest
```

### 旁白

> 接下来展示 rpmsg 多核通信。RK3506G2 有 3 个 Cortex-A7 和 1 个 Cortex-M0，通过硬件邮箱中断实现零拷贝通信，2MB 共享内存窗口映射在 0x03c00000。
>
> M0 固件走的是瑞芯微 SDK 的标准流程：M0 的 rpmsg 固件打成 standalone FIT 镜像放在独立的 amp 分区，U-Boot 启动时在 M0 保持复位的状态下把固件拷进 M0 的 SRAM 再放行；NuttX 作为主核只负责时钟和邮箱收发。
>
> `rpmsgtest` 命令测试 A7 与 M0 的通信链路。发送 "ping from vela" 后，M0 回 "Rockchip rpmsg linux test!"，往返链路即验证通过。
>
> 这条链路我们排查并修复了一系列问题：邮箱/INTMUX 时钟门控的 SET_TO_DISABLE 极性写反、首条 kick 早于 M0 中断武装的竞态（用 M0 独占的就绪位做握手）、M0 时基时钟缺失等。

---

## 第五部分：OTA 升级（3:00 - 3:30）

### 操作命令

```bash
nsh> ota status
nsh> ota confirm
```

### 旁白

> 接下来展示 OTA 升级功能。我们实现了 A/B 双分区热升级机制。
>
> `ota status` 显示当前 A/B 分区状态、启动次数、确认状态。
>
> `ota confirm` 确认当前分区启动成功，防止自动回滚。
>
> 这套机制支持断电安全升级，升级失败自动回滚到上一版本。

---

## 第六部分：文件系统与存储（3:30 - 4:00）

### 操作命令

```bash
nsh> mount
nsh> ls /data
nsh> echo "Hello RK3506" > /data/test.txt
nsh> cat /data/test.txt
nsh> df
```

### 旁白

> 接下来展示文件系统。`mount` 显示已挂载的文件系统：ROMFS 挂载 /etc，littlefs 挂载 /data。
>
> `/data` 使用 littlefs 嵌入式文件系统，支持磨损均衡，适合 SPI NAND Flash。
>
> `echo` 和 `cat` 测试文件读写。`df` 显示磁盘使用情况。

---

## 第七部分：AI 辅助开发（4:00 - 4:20）

### 画面

切换到 AI 对话界面，展示关键调试过程。

### 旁白

> 最后展示 AI 辅助开发。本项目采用人机协作模式，AI 负责代码实现和调试分析，人来拍板技术路线——比如 rpmsg 最终采用 SDK 原生的 U-Boot amp 分区加载，而不是在 NuttX 里自行停核。
>
> 这是网络问题的调试过程。AI 通过源码逐行比对和板上证据，先后定位到 DHCP 的 STP 窗口、DNS nameserver 缺失、RTC 时间与证书有效期、CA 证书包、TLS 熵源等多个互相掩盖的根因。
>
> 截至目前，整个开发过程有 46 个 AI 会话记录：openvela 主仓 158 个提交、vendor/rockchip 子仓 129 个提交，全部开发细节均可追溯。

---

## 结尾（4:20 - 4:30）

### 画面

回到开发板实物，显示 NSH 提示符。

### 旁白

> 以上就是 RK3506G2 openvela 移植的完整展示。感谢观看！

---

## 附录：完整命令列表

### 系统信息

```bash
nsh> uname -a                    # 系统版本
nsh> free                        # 内存使用
nsh> ps                          # 进程列表
nsh> uptime                      # 运行时间
```

### 网络功能

```bash
nsh> ifconfig                    # 网络接口
nsh> ping 192.168.10.1           # 局域网 ping
nsh> ping -c 3 stdl.b4qaq.cn    # DNS 解析 + 外网 ping
nsh> curl --max-time 20 -o /tmp/info.json https://stdl.b4qaq.cn/fwtb/info.json  # HTTPS 下载
nsh> nslookup stdl.b4qaq.cn      # DNS 查询
nsh> route                       # 路由表
```

### USB Host

```bash
# 插入 U 盘后
nsh> ls /dev/sda                 # 块设备
nsh> mount -t vfat /dev/sda /mnt/usb  # 挂载
nsh> ls /mnt/usb                 # 列出文件
nsh> cat /mnt/usb/test.txt       # 读取文件
nsh> umount /mnt/usb             # 卸载
```

### rpmsg 多核

```bash
nsh> rpmsgtest                   # 测试 A7↔M0 通信
nsh> rptun status                # rptun 状态
```

### OTA 升级

```bash
nsh> ota status                  # A/B 分区状态
nsh> ota confirm                 # 确认启动成功
nsh> ota bootcheck               # 开机检查
```

### 文件系统

```bash
nsh> mount                       # 挂载列表
nsh> ls /data                    # 数据分区
nsh> echo "test" > /data/test.txt  # 写文件
nsh> cat /data/test.txt          # 读文件
nsh> df                          # 磁盘使用
```

### 调试命令

```bash
nsh> dmesg                       # 内核日志
nsh> cat /proc/meminfo           # 内存详情
nsh> cat /proc/cpuinfo           # CPU 信息
nsh> date                        # 系统时间
nsh> ntpcstart                   # 启动 NTP 同步
```

---

## 视频录制建议

1. **屏幕录制**: 使用 OBS 或 SimpleScreenRecorder，分辨率 1920x1080
2. **串口终端**: 使用 minicom 或 picocom，字体大小 14pt
3. **实物镜头**: 使用手机或摄像头，展示开发板 LED 和接口
4. **字幕**: 添加中文字幕，方便评委理解
5. **背景音乐**: 可选，音量不要盖过旁白

## 关键展示点

1. **系统启动**: NSH 提示符、内存、进程
2. **网络功能**: DHCP、DNS、curl HTTPS
3. **USB Host**: U 盘挂载、文件读写
4. **rpmsg 多核**: A7↔M0 通信
5. **OTA 升级**: A/B 分区、状态查看
6. **文件系统**: littlefs 读写
7. **AI 辅助**: 调试过程展示
