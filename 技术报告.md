# RK3506G2 的 OpenVela 适配 — 技术报告

## 一、信息表

| 项目 | 内容 |
|------|------|
| 作品名称 | RK3506G2 的 OpenVela 适配 |
| 队伍名称 | 铭诚网络工作室 |
| 团队分工 | 夏嘉铭：主要开发（芯片适配、驱动、系统集成、调试）<br>樊子轩：开发板选型购买，辅助开发（硬件接线、功能测试） |
| 选题方向 | 新硬件平台适配 |

---

## 二、摘要

本作品以"AI + 人工"协作模式，完成了瑞芯微 RK3506G2 芯片（3×Cortex-A7 + 1×Cortex-M0 异构多核、128MB DDR3）在 OpenVela（NuttX 内核）上的**全新芯片级与板级适配**，目标板卡为 HD-RK3506-EVM。

适配前 OpenVela/NuttX 官方代码树中不存在任何 RK3506 支持。本项目从零搭建了完整 BSP：上电引导链（MiniLoader → OP-TEE → U-Boot → NuttX）、24 个芯片级驱动源文件、11 个板级源文件，覆盖串口、I2C、SPI/FSPI、GMAC 以太网、USB Host、VOP 显示、PWM、SARADC、CAN、看门狗、定时器、异构核间通信（rpmsg）与 A/B OTA 升级；网络侧打通 DHCP、DNS、NTP 时间同步与 curl HTTPS（TLS）全链路。

工程数据：OpenVela 主仓 **159 个提交**、rockchip vendor 子仓 **129 个提交**，vendor 层 C 代码约 **2.34 万行**，留存 **46 个 AI Coding 会话日志**，并将移植方法论沉淀为自定义 Skill `openvela-chip-porting`。

---

## 三、正文

### 3.1 绪论

#### 项目背景

RK3506G2 是瑞芯微面向 IoT 的低功耗高性能 SoC：3 个 Cortex-A7（主频可达 1.2GHz）+ 1 个总线 Cortex-M0，128MB DDR3 内置，集成百兆以太网、USB2.0、RGB LCD 接口、CAN-FD、多路 UART/I2C/SPI/PWM/ADC。NuttX 是 POSIX 兼容、裁剪灵活的优秀 IoT 实时操作系统，也是 OpenVela 的内核，但官方代码树尚无 RK3506 系列适配，厂商 SDK 仅提供 Linux/U-Boot/HAL 支持。本项目即填补这一空白：让 RK3506G2 可以运行完整的 OpenVela 系统。

#### 技术难点

1. **零参考项目**：无任何现成 NuttX RK3506 代码，目录结构、链接脚本、时钟树、中断、MMU 均需从零建立。
2. **异构多核**：A7 与 M0 的核间通信依赖硬件邮箱 + INTMUX + 2MB 共享内存，启动顺序、中断握手、M0 固件加载方式均需反复验证。
3. **Rockchip 时钟门控反直觉**：门控寄存器高 16 位为写使能、低 16 位为 SET_TO_DISABLE（写 1 反而是关时钟），极性写反会导致整片外设"假死"。
4. **问题互相掩盖**：典型如 curl HTTPS，先后暴露 DHCP 被交换机 STP 丢弃、无 DNS nameserver、RTC 停在 1970 导致证书"来自未来"、缺 CA 根证书包、缺 `/dev/urandom` 熵源等多个层级问题，单一现象无法独立定位，部分环节必须由人工设计取证实验、AI 无法独立完成。

#### 创新点

1. **AI + 人工分工的新硬件适配范式**：AI 负责大规模代码实现、源码比对与假设生成，人工负责方案拍板、硬件取证与回归验收，在无参考代码的芯片上完成完整 BSP。
2. **完整异构多核支持**：A7 运行 OpenVela 主控、M0 运行 rpmsg 固件，邮箱中断 + 共享内存零拷贝通信。
3. **可复用方法论资产**：沉淀 `openvela-chip-porting` Skill，将"以 Linux SDK 寄存器为事实基准、以已有 NuttX 移植为骨架"的流程固化，可直接迁移到同系列其他芯片。

---

### 3.2 系统方案设计

#### 系统总体架构

```
┌────────────────────────────────────────────────────────┐
│                     应用 / 交互层                        │
│   NSH 命令行 │ LVGL 图形界面 │ rpmsgtest │ curl │ ota    │
├────────────────────────────────────────────────────────┤
│                     系统服务层                          │
│   DHCP/网络配置 │ NTP 时间同步 │ A/B OTA │ KVDB/文件系统 │
├────────────────────────────────────────────────────────┤
│                    NuttX (OpenVela) 内核                │
│   进程调度 │ 内存管理/MMU │ VFS/littlefs │ TCP/IP 网络栈 │
├────────────────────────────────────────────────────────┤
│                    芯片 / 板级驱动层                    │
│ UART I2C SPI FSPI GMAC USB VOP PWM ADC CAN WDT RPTUN   │
├────────────────────────────────────────────────────────┤
│                        硬件层                           │
│  RK3506G2 (3×A7 + 1×M0, 128MB DDR3) + HD-RK3506-EVM   │
│  256MB SPI NAND │ YT8512B PHY │ USB-A │ RGB LCD 接口    │
└────────────────────────────────────────────────────────┘
```

数据流主线：**网络**——应用（curl）经 NuttX 网络栈 → GMAC 驱动 DMA → YT8512B PHY → 以太网；**USB**——块设备 /dev/sda → FAT 文件系统 → USB Host 控制器 → U 盘；**核间**——A7 应用经 rpmsg/virtio → 硬件邮箱中断 → M0 固件处理 → 邮箱回送。

#### 方案论证与选型

- **开发板选型 HD-RK3506-EVM**：瑞芯微官方 EVB，配套资料完整，板载百兆以太网、USB Host、40Pin RM_IO 扩展排针，兼顾芯片适配验证与外设扩展；核心板邮票孔形态也便于后续产品化。
- **操作系统选型 OpenVela/NuttX**：POSIX 兼容性好、驱动上下层分层清晰、组件丰富（LVGL、curl、网络工具链、openamp/rpmsg），相比 Zephyr/FreeRTOS 更契合大赛 OpenVela 生态与 IoT 网关定位。
- **端 / 云职责划分**：端侧（本系统）承担设备接入、本地控制、数据采集与断网缓存；云侧通过 HTTPS（curl + JSON）承担数据汇聚与下发。**断网/弱网降级策略**：DHCP 失败不阻塞启动（重试 10 次覆盖交换机 STP 窗口）、NTP 指数退避后台重试、本地文件系统与核心功能完全离线可用，数据先落 littlefs 的 /data，网络恢复后再同步。
- **M0 加载方式选型（关键决策）**：对比"NuttX 内嵌固件自行拷贝/放行"与"U-Boot 从独立 amp 分区加载"两方案，后者与瑞芯微 SDK 标准流程完全一致（U-Boot 在 M0 复位态拷贝固件并释放），启动职责清晰、避免覆盖运行中固件，最终采用。

#### 关键模块设计

1. **时钟/复位模块**：统一封装 CRU 门控访问，按 SET_TO_DISABLE 语义写"数据 0 + 高位写使能"开时钟；各驱动只声明自身所需门控位。
2. **中断模块**：GICv2 分发器 + CPU 接口，SPI 中断号与 SDK 设备树一一对应；邮箱接收走电平语义 + W1C 清除。
3. **内存/MMU 模块**：128MB DDR3 分区，内核堆、DMA 区、2MB 核间共享窗（0x03c00000，设备/非缓存映射）隔离。
4. **核间协同模块**：邮箱 0/3 + INTMUX 组 3 通道 21 + rpmsg-lite 固件，主从握手与 vring 缓冲按 link-id 0x03 组织。
5. **交互模块**：NSH 全功能命令行 + LVGL 图形界面（480×854）。

---

### 3.3 核心算法与技术原理

#### AI 算法实现

本作品属于**新硬件平台适配赛道**，端侧不部署 AI 推理模型；AI 的角色是开发期智能体（详见 3.6）。系统预留了 OpenVela 的 QuickJS/Lua/MiniBASIC 脚本与图形能力，后续可在 128MB 内存预算内接入端侧模型。

#### 关键机制设计

1. **Rockchip 门控时钟机制**：门控寄存器 `高16位=写使能位掩码，低16位=值`，且 SET_TO_DISABLE——开某时钟须"对应数据位写 0、写使能位置 1"。该机制是本项目多个"外设全无响应"故障的共同根源，形成驱动规范后全量推广。
2. **rpmsg 零拷贝核间通信**：A7 与 M0 共享 2MB 物理内存（vring0/1 + 缓冲池 carveout），数据本身不搬运；核间仅通过邮箱传递"链接号 + RMSG 魔术字"轻量通知。M0 侧 rpmsg-lite 完成远端初始化后等待 link-up，A7 侧 rptun 负责 vring 播种与通知，首条通知必须落在 M0 中断武装之后（以 M0 独占的 INTMUX 使能位作就绪握手）。
3. **USB DWC2 FIFO 配置**：HS 批量传输单包 512B 要求 RX FIFO 容纳约 131 words，默认 128 words 会使控制器静默拒绝发令牌（枚举正常、批量传输死锁）；按 SDK 将 GRX/ NPTX/PTX FIFO 调整为 512/256/224 后修复。
4. **网络多层恢复机制**：DHCP 加长重试覆盖 STP 窗口、NTP 指数退避（1s→120s）解决无电池 RTC 的时间问题、DNS 提供 nameserver 兜底、TLS 使用 BLAKE2s 熵池 + CA 证书包保证 HTTPS 可用。

#### OpenVela 系统能力的深度运用

- **图形**：VOP 帧缓冲驱动（/dev/fb0）+ LVGL（480×854，渲染缓冲 1229760 字节，独立 tick/timer 任务）。
- **网络/多媒体数据通道**：完整 TCP/IP 栈 + curl HTTPS（mbedtls），可直接承载 JSON 业务。
- **系统组件**：littlefs（/data，磨损均衡、掉电安全）、DHCP/DNS/NTP、A/B OTA、openamp/rpmsg。
- **对 OpenVela 的拓展**：新增完整 RK3506 芯片支持与 HD-RK3506-EVM 板级支持（含 GMAC、USB Host、VOP、CAN、WDT、rptun 等驱动）；**改进建议**：可将 Rockchip SET_TO_DISABLE 门控约定与"厂商 SDK 寄存器基准"流程补充进官方芯片移植指南。

---

### 3.4 系统实现

#### 软件 / 固件架构

- **芯片层** `vendor/rockchip/chips/rk3506/`：24 个驱动文件（启动、地址环境、堆栈分配、CRU、IRQ、定时器、lowputc/串口、I2C、SPI、FSPI、SPI NAND、GMAC + HAL、USB Host、VOP、PWM、SARADC、CAN、WDT、rptun、OTA），配套 Kconfig / CMakeLists.txt / Make.defs / hardware 寄存器定义。
- **板级层** `vendor/rockchip/boards/rk3506/hd-rk3506-evm/`：板初始化、bringup、应用注册、网络配置 netcfg、LVGL 显示端口、GT911 触摸、ST7701S 面板、rpmsgtest、ota 命令等 11 个源文件 + nsh defconfig + 链接脚本 + 板级 romfs（含 CA 证书）。
- **引导/打包层** `nand_firmware/`：自建 A/B + AMP 版 U-Boot（随仓提交 uboot.itb）、parameter 分区表、pack_nand.sh 打包、build_mcu_fw.sh 从 SDK HAL 构建 M0 固件并生成 amp.img。

#### 数据流与关键流程

```
冷启动:
MiniLoader → OP-TEE → U-Boot
  U-Boot: 校验/加载自身 FIT → 从 amp 分区加载 M0 固件至 SRAM 并释放
       → 读取 A/B 槽元数据 → 加载 NuttX FIT(kernel+fdt)
NuttX: CRU 开时钟 → 驱动逐个初始化 → 挂载 /etc、/data
       → netcfg: DHCP → DNS → NTP → 进入 NSH
业务:   curl HTTPS / USB 挂载 / LVGL / rpmsgtest / ota
```

#### 硬件设计与适配

- **是否完成全新硬件平台适配/驱动开发：是。**
- 芯片/板卡：Rockchip RK3506G2 / HD-RK3506-EVM；存储 256MB SPI NAND；以太网 PHY 为 YT8512B（RMII，驱动内含 YT8512B 寄存器初始化序列）；USB-A 接 OTG1（Host）。
- 驱动类型：字符设备（UART/PWM/ADC/CAN/看门狗/rpmsg）、网络设备（GMAC）、块设备（SPI NAND/MTD、USB MSC）、显示设备（VOP/fb）。
- 适配难点与解决：① 门控极性写反 → 建立 SET_TO_DISABLE 访问规范；② USB 批量传输死锁 → FIFO 按 SDK 调整 + NAK 重试/500ms 切片等待；③ rpmsg 首通知早于 M0 武装 → INTMUX 就绪握手；M0 link-up 后延时依赖时基时钟 → 补开 TIMER/STCLK 门控；④ /data 文件系统选型 → dhara 磨损均衡 + littlefs（autoformat）。
- 40Pin 扩展排针（RM_IO0~31 可重映射）用于 UART/CAN/SPI 等外设扩展与接线验证。

#### 应用 / 交互端设计

- **NSH 命令行**：`uname/free/ps`、`ifconfig/ping/route`、`curl`、`mount/ls/df`、`ota status/confirm`、`rpmsgtest`、`candump/cansend`、`wtdog` 等。
- **LVGL 图形界面**：480×854 分辨率，`lvgl_homepage` 演示网络状态等 UI；显示经 VOP 帧缓冲 + mmap 渲染。
- 无独立移动端；网关角色下以 HTTPS/JSON 与云端对接。

#### 自定义 Skill（硬性要求）

- **名称**：`openvela-chip-porting`（随工程留存于 `.claude/skills/`）。
- **定义**：新芯片移植到 OpenVela 的完整方法论——参考资料优先级（Linux SDK 寄存器 → 已有 NuttX 同架构移植骨架 → 手册/原理图）、目录与构建系统搭建、最小启动、分阶段外设驱动、调试技巧与真实陷阱案例、提交前检查清单。
- **触发场景**：移植新芯片、新板适配、BSP/bringup、芯片驱动开发、新硬件适配赛道。

---

### 3.5 系统测试与结果分析

#### 测试环境

HD-RK3506-EVM 板卡，USB-TTL 串口控制台（UART0，11500 8N1），百兆局域网（192.168.10.0/24，网关/DNS 192.168.10.1），外网经域名 stdl.b4qaq.cn 验证；烧录采用 `upgrade_tool uf update.img` 全量烧写。

#### 功能测试

| 测试项 | 方法 | 实际结果 |
|--------|------|----------|
| 系统启动/NSH | 冷上电 | 正常进入 `nsh>`，启动横幅与 bringup 完整 |
| 串口控制台 | 命令交互 | 正常 |
| I2C0 | 控制器初始化 | 正常（扫描 GT911；未接面板时 -110 超时属预期） |
| SPI NAND | READ_ID / 容量识别 | `8c a1 8c`，256MiB，2048 块 |
| /data 文件系统 | mount littlefs | 挂载成功，读写正常，首启自动格式化 |
| 以太网/DHCP | 插网线启动 | DHCP 获取地址成功（netcfg） |
| 局域网连通 | ping 网关 | 通，延迟约 2ms |
| DNS/外网 | ping 域名 | 解析正常、连通 |
| NTP 时间 | date | 网络后自动同步，时间正确 |
| curl HTTPS | curl 取 JSON | TLS 握手成功、返回数据、命令正常退出 |
| USB Host MSC | 插 U 盘 + mount vfat | /dev/sda 枚举、挂载、目录/文件读取正常 |
| USB HID/CDC | 设备枚举 | 框架支持 |
| VOP/LVGL | fb0 + lvgl_homepage | /dev/fb0 创建，LVGL 初始化完成（480×854） |
| PWM0 | PWM 输出 | PWM0 ch2 注册 /dev/pwm0 |
| SARADC | /dev/adc0 | 注册成功，通道掩码配置正确 |
| CAN | cansend/candump（回环自测） | can0 初始化、单帧自发自收验证 |
| 看门狗 | wtdog 自动喂狗/不喂狗复位 | /dev/watchdog0/1 注册，喂狗不复位、停喂触发复位 |
| 核间 rpmsg | rpmsgtest | v8h 版本实测收到 "Rockchip rpmsg linux test!"，PASS；后期改为 U-Boot amp 加载流程后继续回归联调 |
| A/B OTA | ota status/confirm/bootcheck | A/B 元数据、槽状态、开机检查正常 |

#### 性能 / 资源实测

| 指标 | 结果 |
|------|------|
| 可用内存（free） | 约 27.5MB（顶部 2MB 让给核间共享窗，属预期） |
| 内核到 NSH 启动时间 | 约 3 秒（U-Boot 加载链约 2s + NuttX 初始化） |
| 局域网 ping 延迟 | 约 2ms |
| USB 大容量顺序读取 | 约 10MB/s 量级 |
| LVGL 显示缓冲 | 单渲染缓冲 1229760 字节，帧缓冲 819840 字节 |
| M0 固件体积 | 27120B（SRAM 窗口 32KiB），amp.img 31744B |
| 固件镜像 update.img | 约 9.4MB |

#### 可靠性与稳定性

- 反复冷/热启动回归：启动链可重复，驱动初始化幂等（单项失败不阻断后续初始化）。
- USB 多次热插拔均能正常枚举/挂载；SPI NAND 坏块由 BBM 识别管理。
- A/B OTA 提供断电安全升级与失败自动回滚兜底；网络全链路具备断网不阻断启动、恢复后自动重连/同步的降级能力。
- 内存在反复启动与功能测试中无异常增长。
- 说明：未做专项 72 小时不间断老化测试，可靠性结论以多轮冷/热启动与功能回归为依据，后续补充长稳测试。

---

### 3.6 AI-Native 开发说明

| 指标 | 数据 |
|------|------|
| AI Coding 代码占比 | 约 80%（口径：vendor 层新增驱动/板级代码及配置、文档由 AI 起草生成；人工负责寄存器事实核对、方案决策与板上验收） |
| 使用的 AI 工具 | MiMoCode（主力开发会话）；DeepSeek Harness / DSH（任务执行与工具编排） |
| MCP 工具使用情况 | 竞赛日志收集（contest-log-collector，自动归集会话）；文件/构建/串口烧录等工具链 |
| Skills 使用与新增 | 使用：openvela-build、nuttx-driver-development 等；**新增沉淀：`openvela-chip-porting`** |
| Token 使用总量 | 未取得精确后台统计，按 46 个会话、约 2.34 万行代码量级估算为百万级 token（建议以 MiMo 控制台实际用量为准） |

**AI 对效率的提升**：驱动骨架/寄存器封装/构建文件可快速批量生成（数倍效率），跨 Linux SDK 与 NuttX 的源码比对、调用栈与根因分析显著加快（疑难问题上十倍量级）。**遇到的问题与解决**：① AI 易违背 NuttX 日志规范（误用其他子系统宏）——在 Skill 中固化规范；② AI 对 Rockchip 门控极性等硬件细节易臆测——强制"只以 Linux SDK 寄存器为事实基准"；③ AI 缺乏硬件上下文——由人工提供寄存器转储/串口日志并设计取证实验，方案级决策一律人工拍板。

---

### 3.7 总结与展望

#### 成果总结

完成 RK3506G2 在 OpenVela 上的**全新芯片 + 板级适配**：引导链完整，24 个芯片驱动 + 11 个板级源文件，覆盖网络（DHCP/DNS/NTP/HTTPS）、USB Host、显示/LVGL、PWM/ADC、CAN、看门狗、rpmsg 多核通信与 A/B OTA；主仓 159 提交、vendor 子仓 129 提交，46 个 AI 会话可追溯，沉淀 1 个可复用 Skill。

#### 应用前景与商业价值

- **目标受众**：IoT 设备厂商与嵌入式开发者（网关、智能家居中控、工业数据采集、边缘节点），面向需要低功耗、多核、联网与丰富接口的产品团队。
- **商业模式**：BSP/驱动技术授权与移植咨询、行业网关整机/方案、开发者培训。
- **规模化潜力**：RK3506 系列出货量大，适配成果与移植方法论可直接迁移到 RK3576/RK3528 等同系芯片，边际成本低。

#### 不足与未来工作

1. 显示面板（ST7701S）与 GT911 触摸因台架未接面板，尚待实物验收。
2. rpmsg 改为 U-Boot amp 标准加载流程后的板上回归需持续收尾。
3. USB Hub 依赖的 asynch 传输尚未实现（直插 U 盘/HID 不受影响）。
4. 音频（I2S）驱动与 OTA 端到端升级、72 小时长稳测试待补充。
5. 后续接入端侧 AI 模型并向 OpenVela 社区回馈上游补丁。

---

## 四、评审维度对照

| 评审维度（分值） | 报告对应章节 | 核心得分依据 |
|------------------|--------------|--------------|
| 技术难度（30） | 3.2 / 3.3 / 3.4 / 3.5 | 零参考全新芯片适配、异构多核、24+ 驱动、多层根因排查 |
| 产品创新性（20） | 摘要 / 3.1 | AI+人工适配范式、完整多核支持、可复用 Skill |
| 项目完整度（20） | 3.5 / 源码 / 展示 | 主仓 159 + vendor 129 提交、约 2.34 万行、功能回归 |
| AI 开发（10） | 3.3 / 3.6 | AI 代码约 80%、自定义 Skill、46 会话可追溯 |
| 商业潜力（10） | 3.7 | IoT 网关/中控、授权咨询、同系芯片可迁移 |
| 展示效果（10） | 演示视频 / 海报 / PPT | 演示视频脚本已就绪，待录制 |
